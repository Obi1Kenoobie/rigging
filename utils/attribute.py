import maya.cmds as cmds


def add_attribute(dag_node, attribute_name, type='float', keyable=True, channel_box=True, lock=False, **kwargs):
    pass


def add_switch(dag_node, attribute_name, keyable=True, lock=False):
    pass


def add_blend_attribute(dag_node, attribute_name, min=0.0, max=1.0, default=1.0, keyable=True, lock=False):
    pass


def add_header_attribute(dag_node, attribute_name):
    pass


def add_proxy_attribute(dag_nodes, attribtue_name, keyable=True, channel_box=True, lock=False, **kwargs):
    pass